# Planned Vite Client

The browser client will live here after the deterministic Python CLI loop works.

For now, this directory is a placeholder so ADR 0002 has a real path without implying that frontend work should start yet.
